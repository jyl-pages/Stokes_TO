cmake_minimum_required(VERSION 3.18)
set(proj_name _install)
project(${proj_name})
include(${PROJECT_SOURCE_DIR}/../../../simplex/script/functions.cmake)
init_project(${PROJECT_SOURCE_DIR}/../../../simplex)
#use_ext_libs(COMPILE "cuda;")
install_all_ext_libs()
set(src_lib_path ${simplex_path}/lib/simplex)
#use_src_libs(COMPILE "common;reservoir;common_cpx;poisson_cpx;viewer;optimization;solver;geometry;physics")
setup_exe_target()
