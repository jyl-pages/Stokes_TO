#include "DirectSolveBlockInverse.cuh"

//__global__ void fuck(Scalar **ptr, Scalar* v0, int A, int N)
//{
//	int i = blockIdx.x*blockDim.x + threadIdx.x;
//	if (i > N) return;
//	ptr[i] = v0 + A * i;
//}
