cmake_minimum_required(VERSION 3.18)
set(proj_name fluid_topo)
project(${proj_name})
set(SIMPLEX_PATH ${PROJECT_SOURCE_DIR}/../../../../../simplex)
include(${SIMPLEX_PATH}/script/functions.cmake)
include(${SIMPLEX_PATH}/script/common_func.cmake)
init_project(${SIMPLEX_PATH})
set(prjplex_path ${PROJECT_SOURCE_DIR}/../../../..)
set(exe_output_path ${prjplex_path}/bin/${platform}/${proj_name})
use_ext_libs(LINK "cuda;openmp;physbam;eigen;")
use_src_libs(SELF "reservoir;optimization;common_cpx")
#target_add_incs(${PROJECT_SOURCE_DIR}/../common)
#target_add_srcs("${current_target}" ${PROJECT_SOURCE_DIR}/../common)
target_add_incs(${PROJECT_SOURCE_DIR}/../stoke)
target_add_srcs("${current_target}" ${PROJECT_SOURCE_DIR}/../stoke)
setup_exe_target()
target_compile_options(${current_target} PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:
						--extended-lambda
						>)
set_property(TARGET ${current_target} PROPERTY CUDA_SEPARABLE_COMPILATION ON)